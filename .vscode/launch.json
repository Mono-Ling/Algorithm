{
    "version": "0.2.0",
    "configurations":
    [
        {
            "name": "(Windows) 启动",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${fileDirname}//Main.exe",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${fileDirname}",
            "environment": [],
            "console": "integratedTerminal",   // 改为集成终端，能看到 cout 输出
            "preLaunchTask": "C/C++: cl.exe 生成活动文件"  // 调试前自动编译
        }
    ]
}